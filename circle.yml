machine:
  python:
    version: 3.4.3

dependencies:
    pre:
        - pip install --upgrade setuptools
        - pip install --upgrade pip
        - pip install pytest

test:
    override:
        - py.test
